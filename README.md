# Projet Symfony Bucket-List

## Étapes de création projet

```shell
composer create-project symfony/web-skeleton bucket-list
cd bucket-list
composer require symfony/apache-pack
composer require webapp
php -S localhost:8000 -t public/
```